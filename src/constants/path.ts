const path = {
  home: '/',
  login: '/login',
  register: '/register',
  discover: '/discover',
  roadmap: '/roadmap',
  profile: '/profile',
  recorder : '/recorder'
} as const

export default path
